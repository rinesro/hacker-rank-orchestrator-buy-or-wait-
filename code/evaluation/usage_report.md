# Token Usage and Cost Report

Generated by `python3 code/evaluation/usage_report.py` from `evaluation/usage_ledger.jsonl`, the append-only ledger that every model call passes through. This report covers **the extraction run that produced the cached evidence in `code/cache/`**, which is the evidence the submitted `output.csv` was built from. It is not a cache-only re-run; cache hits are reported separately below and never replace the extraction figures.

## Provider and model

| Provider | Model | Input $/MTok | Output $/MTok | Surface |
|---|---|---|---|---|
| Anthropic | claude-opus-5 | $5.00 | $25.00 | Messages API, structured output (`output_config.format`) |

One model is used for both call types, so the per-model and overall tables are the same table.

## Calls in the extraction run

| Call type | Calls | Units covered | Executor |
|---|---|---|---|
| vision | 16 | 16 images | see note below |
| messages | 15 | 215 messages | see note below |
| **total** | 31 | - | - |

The ledger holds 1 additional `messages` row(s) beyond the 15 distinct batches: a pilot call covering part of a batch that was later superseded by the full batch. The ledger is append-only, so the superseded attempt is retained rather than deleted.

## Measured token usage

**No API-measured tokens exist for this run.** The environment that produced the cached evidence had no Anthropic credential available — no `ANTHROPIC_API_KEY`, no `ANTHROPIC_AUTH_TOKEN`, no `ant` CLI profile — so the extraction was performed by the coding harness (Claude Code) using the identical prompts in `code/prompts/` and the identical JSON schemas in `code/evidence_ai.py`, and the results were written to `code/cache/` in the same format the SDK path writes.

Those 32 calls are recorded in the ledger with `executor="claude_code_in_session"` and zero token counts, because no Messages API call was made and therefore no `response.usage` block exists to report. Rather than print invented numbers in the measured section, the real per-call input sizes are given as a projection below.

Setting `ANTHROPIC_API_KEY` and running `load_cached_evidence(live=True)` after clearing `code/cache/` exercises the SDK path and fills this section with measured values; no code changes are needed.

| Call type | Ledger rows | Distinct units | Tokens measured | Cost measured |
|---|---|---|---|---|
| vision | 16 | 16 images | n/a - no API call | $0.0000 |
| messages | 16 | 15 batches / 215 messages | n/a - no API call | $0.0000 |

## Cached versus live calls

| Row kind | Ledger rows | Tokens spent | Meaning |
|---|---|---|---|
| `anthropic_sdk` (live) | 0 | 0 | real Messages API call, counted from `response.usage` |
| `claude_code_in_session` | 32 | 0 | extraction run performed by the harness; no API tokens exist |
| `disk_cache` (cache hit) | 0 | 0 | served from `code/cache/`; no model call made |

Every subsequent pipeline run is a **100% cache hit**: all 16 vision units and all 15 message batches resolve from `code/cache/`, so producing `output.csv` again costs zero tokens and is byte-identical.

Note how the cache-hit row above is reached. `load_cached_evidence(live=False)` — the default, and the path `code/main.py` takes — constructs no client at all, so a cache-only run writes **no ledger rows whatsoever**. The ledger therefore cannot be diluted by re-runs: every row in it belongs to the extraction run, which is what makes this report a record of that run rather than of the last time `main.py` was executed. `disk_cache` rows appear only when a live run (`live=True`) finds a unit already cached and skips the call.

This is also what makes the submission reproducible: the current models reject `temperature` and `top_p`, so run-to-run identity comes from the cache, not from sampling settings.

## What a cold run would cost

A cold run is one with `code/cache/` emptied, so every unit is extracted again. Character counts and image pixel counts below are **exact**, read from the real prompts and the real PNGs; only the conversion to tokens is approximate (~3.7 characters per token for text; Anthropic's documented width x height / 750 for images).

| Call type | Calls | Est. input tokens | Est. output tokens | Est. cost |
|---|---|---|---|---|
| vision | 16 | 36,920 | 1,760 | $0.2286 |
| messages | 15 | 45,442 | 19,350 | $0.7110 |
| **total** | 31 | 82,362 | 21,110 | **$0.9396** |

| Metric | Value |
|---|---|
| Exact prompt characters sent | 214,573 |
| Exact image pixels sent | 18,277,820 |
| Requests answered | 250 |
| Est. tokens per request | 413.9 |
| Est. cost per request | $0.003758 |

## Comparison: a naive per-request LLM design

The obvious alternative is one model call per request carrying that user's profile, event history, messages and payment options, and asking for the decision directly. Sized from this dataset's real per-user context:

| Design | Model calls | Input tokens | Output tokens | Est. cost |
|---|---|---|---|---|
| Naive: one call per request | 250 | 683,357 | 100,000 | $5.92 |
| This system: cold run | 31 | 82,362 | 21,110 | $0.9396 |
| This system: cached run | 0 | 0 | 0 | $0.0000 |

The hybrid design makes **31 model calls for 250 requests** instead of 250, an 8x reduction, at roughly **6x lower cost**. The saving is structural rather than a matter of prompt trimming: the model is used only where judgement over unstructured input is actually required — reading 16 scanned documents and classifying 215 free-text messages — while the affordability decision itself is a deterministic simulation that needs no model at all. Cost scales with the amount of unstructured evidence, not with the number of requests, so answering 2,500 requests instead of 250 would not change the extraction bill.

## Ledger schema

Each line of `usage_ledger.jsonl` records: `provider`, `model`, `call_type`, `batch_id`, `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`, `latency_ms`, `cache_hit`, `items`, `executor`, `note` and the derived `cost_usd`. Token fields are copied verbatim from `response.usage`; no field in this report is typed by hand.
